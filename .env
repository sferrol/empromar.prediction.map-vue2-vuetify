#
# https://cli.vuejs.org/guide/mode-and-env.html#environment-variables
#
# Note that only NODE_ENV, BASE_URL, and variables that start with VUE_APP_ will be statically embedded into the client bundle 
# with webpack.DefinePlugin. It is to avoid accidentally exposing a private key on the machine that could have the same name.

# Para probar entorno PRO en local tenemos que indicar las mismas variables que .env.development.local
# Loading order
#   1   .env                ojo que este fichero va al repositorio
#   2   .env.[mode]         overwrite .env 
#   3   .env.[mode].local   overwrite .end.[mode]
# @fake-db: ''
# backend synfony:
# VUE_APP_API_BASE=http://localhost:8050/api/v1
# VUE_APP_API_BASE=https://prediccion.empromar.com
VUE_APP_API_BASE=http://localhost:8050

# Vue APP
VUE_APP_TITLE=emproMar Maps
VUE_APP_BASE=http://localhost:2000

# Debug Mode
VUE_APP_XDEBUG=true